IT Help Desk Support — Landing Page Skeleton

Files
- index.html — main landing page HTML
- styles.css — styles and responsive layout
- script.js — lightweight interactions (reveal on scroll)
- assets/logo.svg — placeholder SVG logo

How to open
1. Open the project folder in your editor.
2. Open `index.html` in a browser (double-click) or run a local server (recommended):

```powershell
cd 'c:\Users\Ador\website'
python -m http.server 5500
# then open http://localhost:5500
```

Notes
- Replace placeholder text, phone, email, and logos as needed.
IT Help Desk Support — Landing Page Skeleton

Files
- index.html — main landing page HTML
- styles.css — styles and responsive layout
- script.js — lightweight interactions (reveal on scroll, background motion)
- assets/logo.svg — placeholder SVG logo

How to open
1. Open the project folder in your editor.
2. Open `index.html` in a browser (double-click or use Live Server extension).

Notes
- This is a static skeleton using only HTML/CSS/JS, responsive and accessible.
- Replace placeholder text, phone, email, and logos as needed.
- The background animation is lightweight and mouse-driven; remove if you need a purely CSS variant.

Next steps
- Add real logo and images in `assets/`.
- Hook up a contact form backend or third-party form provider.
- Add more detailed accessibility attributes and aria labels where needed.
"# faqja" 
