version: '3.8'

services:
  web:
    build: .
    container_name: fieldtech-website
    networks:
      - nginx_network  # Connect to the same network as your nginx proxy
    restart: unless-stopped

networks:
  nginx_network:
    external: true
    name: nginxproxymanager_default  # Name of your nginx proxy manager network